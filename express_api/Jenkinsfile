pipeline {
    agent none
    options {
      	skipDefaultCheckout()
  		disableConcurrentBuilds()
    }

    stages {

      stage('docker-build'){
          agent any

                steps  {

                        checkout scm
                        sh ''' pwd
                         cd express_api
                         pwd '''
                        sh 'make build'

                        archiveArtifacts artifacts: 'docker-tag.txt', onlyIfSuccessful: true
                }
      }

    } // End stages

} // En

